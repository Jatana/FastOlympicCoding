#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <map>
#include <set>
#include <queue>
#define f(i, n) for (int i = 0; i < n; i++)
#define f(i, n, k) for (int i = ; i < n; i += k)


using namespace std;


typedef long long ll;


int main() {
	
	return 0;
}